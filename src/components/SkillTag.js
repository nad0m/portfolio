
import React from 'react';
import { Button } from 'semantic-ui-react';

const style = {
    padding: '8px',
    cursor: 'text',
};

const SkillTag = () => {
    return <Button basic color='teal' content='ReactJS' style={style}/>;
}

export default SkillTag;